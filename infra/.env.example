# Radio Core — minimal API stack
# Copy to infra/.env and replace production passwords.

NODE_ENV=production

# MongoDB is the API's current persistence layer.
MONGODB_ROOT_USER=root
MONGODB_ROOT_PASSWORD=change-me
MONGODB_DB=radiocore

# Redis is reserved for incremental API capabilities.
REDIS_MAX_MEMORY=256mb
