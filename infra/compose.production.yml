name: radio-core

services:
  nginx-proxy:
    restart: always

  radio-core-api:
    restart: always
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 256M

  mongodb:
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M
