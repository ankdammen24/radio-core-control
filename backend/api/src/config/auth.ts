/**
 * Placeholder for a future auth module. No auth provider is required or
 * enforced today — this only reserves the config shape so a real auth
 * module can be added later without restructuring config/.
 */
export const authConfig = Object.freeze({
  enabled: false,
});
