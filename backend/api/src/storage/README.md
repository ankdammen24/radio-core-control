# storage/

Reserved for a future object storage adapter (e.g. R2/S3). Not implemented
yet — no storage provider is required or enforced by the API today. See
`config/storage.ts` for the (currently disabled) config placeholder.
