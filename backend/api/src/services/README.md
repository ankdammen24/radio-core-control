# services/

Reserved for cross-module/shared services (e.g. a future notification or
audit service used by several modules). Per-module business logic lives in
`modules/<name>/<name>.service.ts` next to its repository and routes — see
`modules/stations` for the pattern.

Nothing lives here yet.
